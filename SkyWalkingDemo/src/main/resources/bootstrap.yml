server:
  port: 8050


spring:
  application:
    # 服务名字
    name: skywalking-demo
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8868
      config:
        server-addr: 127.0.0.1:8868
        # 默认properties
        file-extension: yml